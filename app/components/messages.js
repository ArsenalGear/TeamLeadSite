import { defineMessages } from 'react-intl';

export const scope = 'localisation.components';

export default defineMessages({
  clientSelected: {
    id: `${scope}.clientSelected`,
  },
  errorStatus: {
    id: `${scope}.errorStatus`,
  },
  errorMessage: {
    id: `${scope}.errorMessage`,
  },
  errorText: {
    id: `${scope}.errorText`,
  },
});
