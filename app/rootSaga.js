import { all } from 'redux-saga/effects';
// import userLoginActionWatcher from './containers/HomePage/store/saga';

export default function* rootSaga() {
  yield all([
    // getInfoAboutDepositsClientWatcher(),
  ]);
}
